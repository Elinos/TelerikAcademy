using System;

class FiveVariables
{
    static void Main()
    {
        ushort value1 = 52130;
        Console.WriteLine("{0} need to be assigned in ushort variable", value1);
        sbyte value2 = -115;
        Console.WriteLine("{0} need to be assigned in sbyte variable", value2);
        int value3 = 4825932;
        Console.WriteLine("{0} need to be assigned in int variable", value3);
        sbyte value4 = 97;
        Console.WriteLine("{0} need to be assigned in sbyte variable", value4);
        short value5 = -10000;
        Console.WriteLine("{0} need to be assigned in ushort variable", value5);
    }
}
